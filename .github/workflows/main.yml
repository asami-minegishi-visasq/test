name: Deployment

on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo 'build...'
  deployment:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: deploy
        run: echo 'deploy...'
  check:
    runs-on: ubuntu-latest
    steps:
      - name: check
        run: echo 'check...'
